---
import Base from "../../layouts/Base.astro";
import ProductCard from "../../components/ProductCard.astro";
import { safeGetJSON } from "../../lib/api";

const data = await safeGetJSON<{ products: any[] }>("/api/products", { products: [] });
---
<Base title="All Products — Signal & Story">
  <div style="display:flex;align-items:end;justify-content:space-between;gap:10px;flex-wrap:wrap">
    <div>
      <h1 style="margin:0">All products</h1>
      <div style="color:var(--muted);font-size:13px;margin-top:8px">Every artifact in the catalog.</div>
    </div>
    <a class="tag" href="/search">Search</a>
  </div>
  <div style="height:12px"></div>
  {data.products.length ? (
    <div class="grid cols3">
      {data.products.map((p) => <ProductCard product={p} />)}
    </div>
  ) : (
    <div class="card" style="padding:14px">
      <div style="font-weight:800">Catalog service offline</div>
      <div style="color:var(--muted);margin-top:6px">Run <code>npm run dev</code> in <code>apps/api-node</code> or <code>./scripts/dev.sh</code> from the repo root to load products.</div>
    </div>
  )}
</Base>

